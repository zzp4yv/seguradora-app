version: '3.8'

services:
  app:
    build: .
    container_name: seguradora-app
    ports:
      - "3000:3000"
    volumes:
      - ./seguradora.db:/app/seguradora.db
    environment:
      - NODE_ENV=production
    restart: unless-stopped
